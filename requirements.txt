# =============================================================================
# PROSPER-ERP-AUTOMATION - Requirements
# Python 3.12+ - Dependências para automações web (Nodriver + Selenium fallback)
# =============================================================================

# Environment & Config
python-dotenv==1.1.0

# Date & Time
python-dateutil==2.9.0.post0
pytz==2025.2

# Web Automation (Nodriver) - CORE PRINCIPAL
nodriver>=0.33

# Web Automation (Selenium) - FALLBACK/LEGACY (pode remover depois)
selenium==4.27.1
undetected-chromedriver==3.5.5
selenium-stealth==1.0.6
webdriver-manager==4.0.2

# CAPTCHA Solver (CapSolver) - Usando HTTP direto ao invés da biblioteca quebrada
# capsolver==1.0.0  # REMOVIDO - biblioteca quebrada, usando aiohttp direto

# HTTP Requests (async e sync)
aiohttp==3.11.11  # Para API HTTP async do CapSolver
requests==2.32.3  # Para requests síncronos auxiliares
urllib3==2.4.0

# Data Processing (para reporting_utils)
pandas==2.2.3

# Utilities
colorama==0.4.6

# API Flask para controle remoto
Flask==3.0.0

# =============================================================================
# Web Automation (Playwright) - o que os robos USAM hoje
# -----------------------------------------------------------------------------
# Medido em 07/09/2026: o cabecalho acima e da era Nodriver+Selenium (commit 7f8902b,
# out/2025), mas os sete robos que logam no Smart usam PLAYWRIGHT — e tudo o que essa
# migracao precisou foi instalado a mao no container e nunca declarado. O build limpo
# morria em `playwright install chromium` com exit 127, e a imagem em producao (de
# 04/06/2026) nao podia ser reconstruida. Versoes = as instaladas no container que roda.
# =============================================================================
playwright==1.60.0
playwright-stealth==2.0.3      # src/common/browser/stealth_integration.py (import preguicoso)
# Banco (boletos/_db.py, robo_credito/config.py, SQLAlchemy em common/)
psycopg2-binary==2.9.10
SQLAlchemy==2.0.36
# Config e templates (config_loader, notificacoes)
PyYAML==6.0.3
Jinja2==3.1.2
# Leitura de PDF — doc2you/classificar.py le o CNPJ do sacado para NOMEAR a carta.
# Nunca esteve declarado: `texto_pdf` importa pypdf dentro de try/except e devolve ""
# quando falta, entao a carta saia SEM CNPJ no nome (100% das cartas desde 21/08/2026)
# e a verificacao do process-automation passou a ler 550 PDFs por dia para descobrir
# o que o robo ja sabia (17/09/2026). Versao = a do process-automation (uv.lock).
pypdf==6.16.2
